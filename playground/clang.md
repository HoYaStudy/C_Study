# clang (clang 7.0.0)

clang LLVM compiler.

## Options

* `-c` : Only run preprocess, compile, and assemble steps.
* `-D <macro>=<value>` : Define \<macro> to \<value> (or 1 if
\<value> ommitted).
* `-o <file>` : Write output to \<file>.
